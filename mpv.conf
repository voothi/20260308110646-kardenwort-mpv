# Display subtitles on top
sub-align-y=top
sub-margin-y=20

# Support for HTML tags and styles
sub-ass=yes
# sub-ass-override=no

save-position-on-quit=yes

# Always open the window exactly in the center of the screen
geometry=50%:50%

# Limit the maximum window size to 80% of the screen size,
# so that the video never goes beyond the borders of the monitor
autofit-larger=80%x80%
 
# (Optional) If you want the player to ALWAYS open in a specific size, 
# ignoring video resolution (e.g. 1280x720):
autofit=1280x720

# Отвязываем субтитры от границ окна (теперь они привязаны только к самому видео)
sub-scale-with-window=no
sub-ass-scale-with-window=no

# Увеличиваем или уменьшаем базовый размер шрифта из файла субтитров
# 1.0 - оригинальный размер, 1.5 - больше на 50%, 0.8 - меньше на 20%
sub-scale=1.5