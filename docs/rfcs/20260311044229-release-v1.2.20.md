# RFC: 20260311044229

## Title: Release v1.2.20 - Regression Audit & Release Documentation

### Overview
This release formalizes the comprehensive regression audit between commits `abf23f4` and `27c1e76`, confirming zero regressions across the 18-commit Mouse Selection feature set. It also packages the full release documentation (RFC, release notes, README updates) for the v1.2.18 feature drop.

### Implementation Details

#### Regression Audit: `abf23f4` → `27c1e76`

**Scope**: 1 file changed (`scripts/lls_core.lua`), +398/-46 lines, 18 commits, 10 diff hunks.

##### Hunk-by-Hunk Verdict

| # | Lines | Change | Regression? |
|---|---|---|---|
| 1 | Options table | Added `dw_font_name`, `dw_char_width` — trailing comma fix on `dw_highlight_color` | ✅ Clean |
| 2 | FSM table | Added `DW_MOUSE_DRAGGING`, `DW_MOUSE_SCROLL_TIMER` — trailing comma fix on `DW_KEY_OVERRIDE` | ✅ Clean |
| 3 | After `draw_drum` | **New**: `dw_get_str_width`, `dw_build_layout` — pure additions, no existing code touched | ✅ Clean |
| 4 | `draw_dw` refactor | Uses layout engine, `\q2` instead of `\q0`, added `\fn` tag. Selection highlighting logic **structurally identical** (same `has_selection`, `p1_l/p1_w/p2_l/p2_w` comparisons, same `goto next_word` pattern). Only change: now loops over `entry.vlines` wrapping. | ✅ Clean |
| 5 | After `draw_dw` | **New**: `dw_get_mouse_osd`, `dw_hit_test`, `dw_mouse_update_selection`, `dw_mouse_auto_scroll`, `make_mouse_handler`, `cmd_dw_double_click` — pure additions | ✅ Clean |
| 6 | `tick_drum` | Added early return guard `if DRUM_WINDOW ~= "OFF"` — prevents drum OSD from competing with DW | ✅ Clean |
| 7 | `manage_dw_bindings` keys | Added `Ctrl+UP/DOWN`, `MBTN_LEFT`, `Shift+MBTN_LEFT`, `MBTN_LEFT_DBL`, `Ctrl+ВВЕРХ/ВНИЗ` — pure additions to the `keys` table | ✅ Clean |
| 8 | `manage_dw_bindings` logic | Refactored binding registration to handle `complex=true` for mouse bindings separately. Added cleanup block for mouse state + window-dragging save/restore. **Original `repeatable` logic preserved exactly.** | ✅ Clean |
| 9 | `cmd_toggle_drum_window` | Added subtitle visibility snapshot on open (`DW_SAVED_SUB_VIS`, etc.) and restore on close. **All original DW lifecycle logic untouched** (subs boot, cursor init, DW_FOLLOW_PLAYER). | ✅ Clean |
| 10 | `cmd_dw_line_move` | Changed edge-scroll from `+dir` drift to directional snap (`cursor + half` / `cursor - half`). **Only the viewport centering math changed**, cursor movement logic untouched. | ✅ Clean |

##### Functions Verified Completely Untouched
`cmd_dw_copy`, `cmd_dw_word_move`, `cmd_dw_scroll`, `cmd_toggle_drum`, `draw_drum`, `tick_dw`, `tick_drum` (except guard), `tick_autopause`, `master_tick`, `cmd_smart_space`, `cmd_toggle_sub_vis`, `cmd_cycle_sec_pos`, all copy functions.

**Verdict: Zero regressions. All changes are additive or precisely scoped refactors with identical logic.**

#### Documentation Updates
- **RFC**: Created `docs/rfcs/20260311023622-release-v1.2.18.md` documenting all technical decisions (layout engine, hit-testing, OS conflict resolution, hardware-accelerated dragging).
- **Release Notes**: Added v1.2.18 "Advanced Mouse Selection" section to `release-notes.md` covering Mouse Selection, UI Conflict Resolution, and Synchronized Scrolling.
- **README**: Bumped version badge to v1.2.18. Expanded "Static Reading Mode" section with Advanced Mouse Selection and Actionable Text (double-click seek). Added `LMB (Drag)`, `LMB (Double)`, and `Ctrl+UP/DN` to the keybindings table.

### Analytics
This is a documentation-only release with no code changes to `lls_core.lua`.
