# Прокрутка стрелками влево/вправо ровно на 2 секунды
RIGHT seek  2 exact
LEFT  seek -2 exact

# Копирование текста текущих субтитров в буфер обмена по нажатию Ctrl+C
# Ctrl+c set clipboard "${sub-text}"

a sub-seek -1
d sub-seek  1

s cycle sub-visibility

# p screenshot