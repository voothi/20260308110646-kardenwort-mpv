# Scroll left/right arrows for exactly 2 seconds
RIGHT seek  2 exact
LEFT  seek -2 exact

# Copy the text of the current subtitles to the clipboard by pressing Ctrl+C
# Ctrl+c set clipboard "${sub-text}"

a sub-seek -1
d sub-seek  1

s cycle sub-visibility

S screenshot
