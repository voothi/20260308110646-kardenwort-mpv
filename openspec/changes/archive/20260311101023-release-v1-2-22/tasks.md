## 1. Migration Verification

- [ ] 1.1 Verify `A`/`D` and `Ф`/`В` bindings in `input.conf` for `seek -2 exact` and `seek 2 exact`
- [ ] 1.2 Confirm these keys function correctly while the Drum Window is active
- [ ] 1.3 Verify that the keys do not interfere with viewport scrolling
- [ ] 1.4 Validate that the seekers use the `exact` flag for precision
