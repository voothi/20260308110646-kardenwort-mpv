# Tasks: Centralized Config & Styling

## 1. Configuration Migration
- [x] Relocate `lls_core.lua` parameters to `mpv.conf`
- [x] Implement `mp.options.read_options` in `lls_core.lua`
- [x] Verify settings override hardcoded defaults

## 2. Visual Standardization
- [x] Standardize header width to 75 characters in `.conf` files
- [x] Unified subsection marker styling (`# --- Title ---`)
- [x] Normalize vertical spacing and alignment

## 3. Documentation & templates
- [x] Restore and standardize `(Template)` entries in `mpv.conf`
- [x] Document language priority and scaling override templates
- [x] Ensure `input.conf` preserves Russian layout bindings

## 4. Stability
- [x] Verify local `Options` fallback mechanism
- [x] Validate syntax of updated `mpv.conf` and `input.conf`
