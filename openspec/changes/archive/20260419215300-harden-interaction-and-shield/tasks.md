# Tasks: Interaction Shielding

- [x] Add `DW_MOUSE_LOCK_UNTIL` to `FSM` table.
- [x] Implement the 150ms lock trigger in `parse_and_bind` keyboard callbacks.
- [x] Implement the lock check in `make_mouse_handler`.
- [x] Harden `cmd_dw_toggle_pink` with strict context isolation.
- [x] Verify that remote `t` no longer causes pointer jumps.
