## 1. Code Cleanup

- [x] 1.1 Remove duplicate `book_mode` entry from the `Options` table in `scripts/lls_core.lua`.

## 2. Configuration Synchronization

- [x] 2.1 Add the following missing options to `mpv.conf`: `osd_interactivity`, `dw_scrolloff`, `dw_active_opacity`, `dw_context_opacity`, `dw_key_cycle_copy_mode`, `dw_key_toggle_copy_context`.
- [x] 2.2 Add descriptive comments for `osd_interactivity` and `dw_scrolloff` in `mpv.conf`.
- [x] 2.3 Verify that all values match the script's defaults or preferred user settings.
