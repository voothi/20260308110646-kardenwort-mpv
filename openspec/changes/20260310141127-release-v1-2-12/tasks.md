## 1. Migration Verification

- [ ] 1.1 Verify `Shift+R`/`Shift+T` and `К`/`Е` bindings for `secondary-sub-pos` in `input.conf`
- [ ] 1.2 Confirm `r`/`t` and `к`/`е` bindings for `sub-pos` in `input.conf`
- [ ] 1.3 Verify that manual positioning offsets are respected when toggling Drum Mode
- [ ] 1.4 Test independent track movement to ensure no collision between primary and secondary tracks
